cp .gitkeep 抽象工厂模式/
cp .gitkeep Builder模式/
cp .gitkeep 工厂方法模式/
cp .gitkeep 对象池模式/
cp .gitkeep 单例模式/
cp .gitkeep 适配器模式/
cp .gitkeep 桥接模式/
cp .gitkeep 合成模式/
cp .gitkeep 装饰模式/
cp .gitkeep 门面（Facade）模式/
cp .gitkeep Flyweight模式/
cp .gitkeep MVC模式/
cp .gitkeep 代理模式/
cp .gitkeep 责任链模式/
cp .gitkeep 命令模式/
cp .gitkeep 中介（Mediator）模式/
cp .gitkeep 观察者模式/
cp .gitkeep 注册（Registry）模式/
cp .gitkeep 状态模式/
cp .gitkeep 策略模式/
cp .gitkeep 模板（Template）模式/
cp .gitkeep 访问者模式/
cp .gitkeep 条件变量/
cp .gitkeep Lock/Mutex/
cp .gitkeep 监视器模式/
cp .gitkeep 读写锁定模式/
cp .gitkeep Semaphore/
cp .gitkeep 扇入（Fan-In）/
cp .gitkeep 扇出（Fan-Out）/
cp .gitkeep Futures & Promises/
cp .gitkeep Publish/Subscribe/
cp .gitkeep Push & Pull/
cp .gitkeep Bulkheads/
cp .gitkeep 断路器（Circuit-Breaker）模式/
cp .gitkeep 截止日期（Deadline）/
cp .gitkeep Fail-Fast机制/
cp .gitkeep Handshaking/
cp .gitkeep 稳定状态（Steady-State）/
cp .gitkeep Timing Functions/
cp .gitkeep Functional Options/
cp .gitkeep 级联故障/
